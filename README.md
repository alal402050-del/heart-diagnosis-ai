# Heart Diagnosis AI (FastAPI)

مشروع ذكاء اصطناعي لتصنيف احتمالية الإصابة بمرض القلب (Supervised Learning - Classification) باستخدام نموذج تم تدريبه مسبقًا، مع واجهة ويب بسيطة وواجهة API عبر FastAPI.

## ✅ الروابط بعد النشر
- الصفحة الرئيسية (واجهة المستخدم): `/`
- Swagger UI: `/docs`
- ReDoc: `/redoc`

## 🧠 فكرة المشروع
يستقبل النظام بيانات طبية مثل: العمر، الجنس، نوع ألم الصدر، ضغط الدم، الكوليسترول، نبض القلب الأقصى… ثم يرجع نتيجة تصنيف:
- `label = 0` : لا يوجد مرض قلبي
- `label = 1` : احتمال وجود مرض قلبي
مع إرجاع احتمال (Probability) إن توفر.

> ملاحظة: التطبيق لا يحتاج مجموعة البيانات للتشغيل لأن النموذج مُدرّب مسبقًا ومضمّن داخل الكود.

## 🧩 التقنيات المستخدمة
- Python
- FastAPI + Uvicorn
- scikit-learn (Model)
- Render (Deployment)
- GitHub

## 🔌 API
### POST `/predict`
**Request JSON Example**
```json
{
  "Age": 55,
  "Sex": "M",
  "ChestPainType": "ATA",
  "RestingBP": 130,
  "Cholesterol": 250,
  "MaxHR": 150,
  "ExerciseAngina": "N",
  "Oldpeak": 1.2,
  "ST_Slope": "Up"
}
```

**Response Example**
```json
{
  "label": 1,
  "probability": 0.82
}
```

## ▶️ تشغيل المشروع محليًا
1) تثبيت المتطلبات:
```bash
pip install -r requirements.txt
```

2) تشغيل السيرفر:
```bash
uvicorn app:app --reload
```

ثم افتح:
- http://127.0.0.1:8000/

## 🌍 إعداد Render
- **Build Command**
```bash
pip install -r requirements.txt
```
- **Start Command**
```bash
uvicorn app:app --host 0.0.0.0 --port 10000
```

## ⚠️ تنبيه
هذا المشروع لأغراض تعليمية/بحثية ولا يُعد بديلًا عن التشخيص الطبي المتخصص.
